# true = in-memory mock in src/services/mock. false = Master Edge Functions.
VITE_USE_MOCK=true

# Master (registry) project. Both values are public: the registry anon key is
# the same one the Tabstr POS ships. Put the key in .env.local — never commit
# it. No service_role key, no per-tenant anon key and no RPC secret belongs
# in a VITE_* variable.
VITE_MASTER_FUNCTIONS_URL=https://itevgnkkwkufjbiuhnlj.supabase.co/functions/v1
VITE_MASTER_ANON_KEY=
